diff --git a/dist/index.cjs b/dist/index.cjs
index 2401507b94c1a9c4e03d85e46871fd95d444f777..ee59061323226f2f730c799fe69e78ee0c75c2d1 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -154,5 +154,15 @@ function getPackageDir() {
   return (0, import_node_path.dirname)(currentDir);
 }
+function resolveUnpackedAsarPath(binaryPath) {
+  const pathSegments = binaryPath.split(import_node_path.sep);
+  const asarIndex = pathSegments.findLastIndex((segment) => segment.endsWith(".asar"));
+  if (asarIndex === -1) {
+    return binaryPath;
+  }
+  pathSegments[asarIndex] = `${pathSegments[asarIndex]}.unpacked`;
+  const unpackedPath = pathSegments.join(import_node_path.sep);
+  return (0, import_node_fs.existsSync)(unpackedPath) ? unpackedPath : binaryPath;
+}
 function binaryExists() {
   return findBinary() !== null;
 }
@@ -164,7 +174,7 @@ function resolveFromNpmPackage() {
     const packageDir = (0, import_node_path.dirname)(packageJsonPath);
     const binaryPath = (0, import_node_path.join)(packageDir, getLibFilename());
     if ((0, import_node_fs.existsSync)(binaryPath)) {
-      return binaryPath;
+      return resolveUnpackedAsarPath(binaryPath);
     }
   } catch {
   }
diff --git a/dist/index.js b/dist/index.js
index 210fbdcb61d00f0b7d05a33bc9bbeaec251c5076..8413630078675119802b5298694572576d266613 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -2,4 +2,4 @@
 import { existsSync, readFileSync } from "fs";
 import { createRequire } from "module";
-import { dirname, join } from "path";
+import { dirname, join, sep } from "path";
 import { fileURLToPath } from "url";
@@ -116,5 +116,15 @@ function getPackageDir() {
   return dirname(currentDir);
 }
+function resolveUnpackedAsarPath(binaryPath) {
+  const pathSegments = binaryPath.split(sep);
+  const asarIndex = pathSegments.findLastIndex((segment) => segment.endsWith(".asar"));
+  if (asarIndex === -1) {
+    return binaryPath;
+  }
+  pathSegments[asarIndex] = `${pathSegments[asarIndex]}.unpacked`;
+  const unpackedPath = pathSegments.join(sep);
+  return existsSync(unpackedPath) ? unpackedPath : binaryPath;
+}
 function binaryExists() {
   return findBinary() !== null;
 }
@@ -128,7 +138,7 @@ function resolveFromNpmPackage() {
     const packageDir = dirname(packageJsonPath);
     const binaryPath = join(packageDir, getLibFilename());
     if (existsSync(binaryPath)) {
-      return binaryPath;
+      return resolveUnpackedAsarPath(binaryPath);
     }
   } catch {
   }
